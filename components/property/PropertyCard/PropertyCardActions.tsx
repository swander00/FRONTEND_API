type PropertyCardActionsProps = {
  onViewDetails?: () => void;
  onContact?: () => void;
};

export function PropertyCardActions(props: PropertyCardActionsProps) {
  void props;
  // Actions are currently handled by clicking the card itself
  // This component can be extended for additional actions if needed
  return null;
}

