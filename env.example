# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================
# 
# Copy this file to .env.local and fill in your actual values
# Next.js will automatically load .env.local for local development
#
# IMPORTANT: All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Never put sensitive secrets in NEXT_PUBLIC_ variables
# ===========================================

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================

# Application URL (optional, used for redirects and callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# SUPABASE (REQUIRED)
# ===========================================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

# Supabase Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous (Public) Key
# This is safe to expose to the browser - it's protected by Row Level Security (RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-public-anon-key

# ===========================================
# BACKEND API (REQUIRED)
# ===========================================
# Your backend API URL for authenticated requests

# Primary API URL (recommended for new code)
# For local development: http://localhost:8080
# For production: https://your-backend.railway.app (or your production URL)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Legacy API URL (for backward compatibility)
# Only needed if you have legacy code still using this variable
NEXT_PUBLIC_BACKEND_URL=https://your-backend.railway.app

# ===========================================
# GOOGLE SERVICES
# ===========================================

# Google Maps API Key (REQUIRED for map functionality)
# Get this from Google Cloud Console: https://console.cloud.google.com/google/maps-apis/credentials
# Make sure to enable the following APIs:
# - Maps JavaScript API
# - Places API (if using place search)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Google Maps Map ID (REQUIRED for AdvancedMarkerElement)
# Create a Map ID in Google Cloud Console: https://console.cloud.google.com/google/maps-apis
# For testing, you can use "DEMO_MAP_ID" but it's recommended to create your own Map ID
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=your-map-id-or-DEMO_MAP_ID

# Google OAuth Client ID (REQUIRED for Google Sign-In and One Tap)
# Get this from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# Must be the SAME Client ID configured in Supabase Authentication → Providers → Google
# Format: 123456789-abc...xyz.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Google Analytics ID (OPTIONAL)
# Only needed if you're using Google Analytics tracking
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=your-google-analytics-id

# ===========================================
# NOTES
# ===========================================
# 
# 1. After creating .env.local, restart your Next.js dev server
# 2. For production deployments (Vercel, etc.), set these variables in your platform's environment settings
# 3. Never commit .env.local to Git - it's already in .gitignore
# 4. All NEXT_PUBLIC_ variables are bundled into your client-side code
# 5. For sensitive server-side variables, use regular env vars (without NEXT_PUBLIC_ prefix)
#    and access them only in API routes or getServerSideProps/getStaticProps
